# Deye Solarman Local

Reserved application directory for the future local Solarman TCP add-on.

Do not add `config.yaml`, a Dockerfile, or runtime files until the shared core extraction and the compatibility migration are ready. The released implementation remains in `../../deye-solarman-diagnostics/`.
