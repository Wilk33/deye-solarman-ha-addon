# Deye RS485 Local

Reserved application directory for the future direct Modbus RTU add-on.

This directory intentionally contains no Home Assistant application configuration. It becomes installable only after the shared core and a tested RS485 transport adapter exist.
